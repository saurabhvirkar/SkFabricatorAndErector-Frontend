export interface Project {
  id: number;
  title: string;
  category?: string;
  description?: string;
  image?: string;
  publicId?: string;
  categoryLabel?: string;
  client?: string;
  photoPlaceholder?: string;
}
